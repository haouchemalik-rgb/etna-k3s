apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wp-http
  namespace: blog
spec:
  entryPoints:
  - http
  routes:
  - match: Host(`web.etna.student`)
    kind: Rule
    services:
    - name: wordpress
      port: 80
